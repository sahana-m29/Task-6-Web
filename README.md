# Task-6-Web
# Contact Form with Validation

This project is a simple **HTML, CSS, and JavaScript Contact Form** with
real‑time validation.

## 📌 Features

-   Validates **Name**, **Email**, and **Message**
-   Email is validated using **regular expression**
-   Shows **error messages** for invalid inputs
-   Shows a **success message** when the form is valid
-   Clean and modern UI created using CSS

## 🚀 How It Works

1.  User fills out the fields.
2.  JavaScript checks:
    -   Name must not be empty\
    -   Email must follow a valid format\
    -   Message must not be empty\
3.  If any field is invalid → error message is shown.
4.  If all fields are valid → success message appears.

## 📂 File Structure

    project-folder/
    │-- index.html
    │-- README.md

## 🖥️ Technologies Used

-   **HTML** -- Form structure\
-   **CSS** -- Styling\
-   **JavaScript** -- Validation logic

## 📝 How to Use

1.  Save the HTML code as `index.html`
2.  Open it in your browser\
3.  Try submitting the form with empty inputs to see validation\
4.  Fix inputs and submit again to see the success message

## 💡 Notes

-   This is a **frontend‑only form**.\
-   It does not send data to a backend server.\
-   You can integrate it with **Flask/PHP/Node.js** if needed.
